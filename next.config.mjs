/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['is1-ssl.mzstatic.com'], // Adicione o domínio da imagem aqui
  },
};

export default nextConfig;
